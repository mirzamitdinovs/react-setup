import React from 'react';
import TodosList from '../../containers/TodosList';

const TodosPage = () => {
	return (
		<div className='p-10'>
			<TodosList />
		</div>
	);
};

export default TodosPage;
